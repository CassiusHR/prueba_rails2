<nav class="navbar navbar-expand-lg navbar-light bg-light">
<a class="navbar-brand" href="<%= root_path %>">Todo List</a>
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
</button>

<div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav mr-auto">
  </ul>
  <ul class="nav navbar-nav navbar-right p-2">
      <% if user_signed_in? %>
      <li class="p-1">
        <div style="width:50px;height:50px;border-radius:50%;overflow:hidden;border:2px solid #ccc;">
          <img src="<%= current_user.photo %>" alt="" style="max-height:100%;">
        </div>
      </li>
      <li class="p-1">
        <div style="display:flex;flex-direction:column;justify-content: flex-start;">
        <h3 class="m-0"><%= current_user.name %></h3>
        <p class="m-0"><%= current_user.email %><p>
        </div>
      </li>
    <% else %>
    <li class="p-1"> Ingreso</li>
    <% end %>
      <% if user_signed_in? %>
      <li class="p-1">
        <a href="<%= destroy_user_session_path %>" data-method="DELETE"> <button class="btn btn-primary btn-sm">Salir</button> </a>
      </li>
      <% else %>
      <li class="p-1">
      <a href="<%= new_user_session_path %>">Ingresar</a>
      </li>
      <li class="p-1">|</li>
      <li class="p-1">
        <a href="<%= new_user_registration_path %>">Registrarse</a>
      </li>
    <% end %>
  </ul>
</div>
</nav>